# https://dart.dev/guides/libraries/private-files
.dart_tool/
build/

# A package that is published does not pin its dependencies.
pubspec.lock

# Generated by .github/tool/detach_workspace.sh so CI analyses the example
# against the working tree rather than the last release. The example's own
# pubspec depends on a published version, which is the thing being
# demonstrated, so this redirect must stay out of git.
example/pubspec_overrides.yaml
